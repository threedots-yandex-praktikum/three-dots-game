export type TProfileProps = Record<string, unknown>;

export type TInitialFormState = {
  login: string,
  email: string,
  phone: string,
  first_name: string,
  second_name: string,
  display_name: string,
};
